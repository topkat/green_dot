/**------------------------------------------------------------
This file should be in the root of your app folder. Usually 
where the `green_dot.APP.config.ts` file resides. It is here 
to correctly scope the global types inside your app
---------------------------------------------------------------*/
{
  "extends": "../tsconfig.jsonc",
  "include": [
    "./**/*"
  ]
}